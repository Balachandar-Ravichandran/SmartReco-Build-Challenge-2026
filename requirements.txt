# PATHWISE — Requirements
# Production dependencies (Mesh API only, competition-safe)
# Version floors only (no exact pins) so pip resolves modern wheels with
# prebuilt binaries for current Python — exact pins here previously forced
# source builds of pydantic-core / chroma-hnswlib requiring a Rust toolchain.

# Web Framework
fastapi>=0.104
uvicorn[standard]>=0.24
jinja2>=3.1
python-multipart>=0.0.9

# Database
sqlalchemy>=2.0
# SQLite driver included in Python stdlib

# Vector Store
chromadb>=0.5,<0.6

# Agent Orchestration
langgraph>=0.0.45
langchain-core>=0.1.52

# Data Validation
pydantic>=2.5
pydantic-settings>=2.1

# LLM API Client (Mesh API compatible)
openai>=1.3

# Scheduling
apscheduler>=3.10

# Utilities
python-dotenv>=1.0
httpx>=0.25
aiofiles>=23.2

# Observability (optional bonus)
langsmith>=0.1

# Development & Testing
pytest>=7.4
pytest-asyncio>=0.21
pytest-cov>=4.1
